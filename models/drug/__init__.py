class Drug:
    def __init__(self, name, ingredients, usage, use_method, scale, number, enterprise, img_url):
        self.name = name
        self.ingredients = ingredients
        self.usage = usage
        self.use_method = use_method
        self.scale = scale
        self.number = number
        self.enterprise = enterprise
        self.img_url = img_url